apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - networkpolicy.yaml
  - poddisruptionbudget.yaml
  - service.yaml
  - inferencepool.yaml

configMapGenerator:
  - name: xllm-service-runtime
    envs:
      - runtime.env

commonLabels:
  app.kubernetes.io/name: xllm-service
  app.kubernetes.io/component: adapter
