apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: xllm-service
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: xllm-service
      app.kubernetes.io/component: adapter
