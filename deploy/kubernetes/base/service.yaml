apiVersion: v1
kind: Service
metadata:
  name: xllm-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: xllm-service
    app.kubernetes.io/component: adapter
  ports:
    - name: http
      port: 8888
      targetPort: http
      protocol: TCP
