include(cc_binary)
include(cc_library)
include(cc_test)

cc_library(
  NAME
  xllm_http_service
  HDRS
    service.h
    request_tracer.h
  SRCS
    service.cpp 
    request_tracer.cpp
  DEPS
    :backend_http
    :common
    :dispatcher
    :request
    :runtime
    :scheduler
    :telemetry
    absl::random_random
    absl::synchronization
    glog::glog
    nlohmann_json::nlohmann_json
    proto::proto_http_service
    proto_xllm
)
target_link_libraries(xllm_http_service PRIVATE brpc-static leveldb::leveldb ZLIB::ZLIB protobuf::libprotobuf OpenSSL::SSL OpenSSL::Crypto)

cc_test(
  NAME
    health_service_test
  SRCS
    health_service_test.cpp
  DEPS
    :xllm_http_service
    GTest::gtest_main
)

cc_binary(
  NAME 
    xllm_http_serving
  SRCS 
    main.cpp
  DEPS
    :xllm_http_service
    gflags::gflags
)
